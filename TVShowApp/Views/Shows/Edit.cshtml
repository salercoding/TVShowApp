@model TVShowApp.Models.Show

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Show</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ShowTitle" class="control-label"></label>
                <input asp-for="ShowTitle" class="form-control" />
                <span asp-validation-for="ShowTitle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ShowDescription" class="control-label"></label>
                <input asp-for="ShowDescription" class="form-control" />
                <span asp-validation-for="ShowDescription" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
